---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: "{{ lsyncd_apt_update_cache_valid_time }}"
  register: lsyncd_apt_cache_update
  retries: 3
  delay: 10
  until: lsyncd_apt_cache_update is success

- name: Install packages
  ansible.builtin.apt:
    name: "{{ lsyncd_apt_packages }}"
    state: present
    update_cache: no
  register: lsyncd_apt_install_result
  retries: 3
  delay: 10
  until: lsyncd_apt_install_result is success

- name: Check if reboot is required
  ansible.builtin.stat:
    path: /var/run/reboot-required
  register: lsyncd_reboot_required_file

- name: Notify if reboot is required
  ansible.builtin.debug:
    msg: "⚠️  Reboot required on {{ inventory_hostname }}"
  when: lsyncd_reboot_required_file.stat.exists
